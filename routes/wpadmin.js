var express = require('express');
var router = express.Router();

/* Handle POST: Math the user name and password. */
router.get('/', function(req, res, next) {
  // res.render('wpadmin', { title: 'Express' });
});

module.exports = router;
